import { StyleSheet } from 'react-native';

// import { colors, fonts } from 'Theme';

export default StyleSheet.create({
  headerStyle: {
    // backgroundColor: colors.white,
  },
  headerTitleStyle: {
    // color: colors.black,
    // fontSize: fonts.fontSize.subHeading,
    // fontWeight: fonts.fontWeight.bold,
  },
});